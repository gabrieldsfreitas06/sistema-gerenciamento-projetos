package model;

public class GerenteProjeto extends model.Usuario {

    public GerenteProjeto(String nome,
                          String email,
                          String senha) {

        super(nome, email, senha);
    }
}